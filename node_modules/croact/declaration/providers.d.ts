import { Provider } from "./base/Provider";
import { CompatElement } from "./types";
export declare class ContainerProvider extends Provider<any> {
    typ: string;
    constructor(base: Element, depth?: number);
    r(): boolean;
    un(): void;
}
export declare class TextProvider extends Provider<Node> {
    typ: string;
    r(hooks: Function[]): boolean;
    un(): void;
}
export declare function diffProviders(containerProvider: Provider, providers: Provider[], children: Array<CompatElement | string>): number[];
